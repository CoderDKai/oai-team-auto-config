# 模块依赖规则

## 依赖方向

- 模块依赖必须单向，禁止双向依赖
- 依赖方向以业务能力层级为准，基础能力（core）可被依赖，但不依赖业务模块

## 允许的依赖类型

- 调用对方公开接口或函数（由模块负责人维护）
- 通过明确的数据结构或契约共享信息

## 禁止的依赖类型

- 直接访问其他模块的内部实现或私有数据
- 形成循环依赖
- 未经评审的跨模块调用

## 变更要求

- 新增依赖必须在变更说明中标注
- 涉及跨模块依赖的变更必须经过相关模块负责人评审