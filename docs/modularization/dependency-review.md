# 依赖评审清单

## 评审前检查

- 变更说明是否标注新增/修改的依赖模块
- 依赖方向是否符合单向依赖规则
- 是否涉及 core 以外的跨模块调用

## 依赖合规检查

- 是否仅调用对方公开接口或约定的数据结构
- 是否直接引用对方内部实现（若有则拒绝）
- 是否存在循环依赖风险

## 影响范围

- 变更是否需要同步更新依赖模块的文档或契约
- 是否需要相关模块负责人参与评审
- 是否需要更新模块清单或依赖规则

## 结论

- 通过 / 需修改 / 拒绝
- 评审人签名与日期